
apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: redis-slave-pr
  namespace: guestbook
  labels:
    app: redis-slave-app
spec:
  clusterReplicas: 2
  clusterConditions:
    - type: ManagedClusterConditionAvailable
      status: "True"
  clusterSelector:
    matchLabels:
      environment: qa
      #vendor: OpenShift
